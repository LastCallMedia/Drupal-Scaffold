<h3>Atoms</h3>
<ul>
{% for pattern in patterns.atom %}
  <li><a href="{{ url('pattern.view', { patternId: pattern.getId() }) }}">{{ pattern.getName() }}</a></li>
{% endfor %}
</ul>
<h3>Molecules</h3>
<ul>
  {% for pattern in patterns.molecule %}
    <li><a href="{{ url('pattern.view', { patternId: pattern.getId() }) }}">{{ pattern.getName() }}</a></li>
  {% endfor %}
</ul>
<h3>Elements</h3>
<ul>
  {% for pattern in patterns.element %}
    <li><a href="{{ url('pattern.view', { patternId: pattern.getId() }) }}">{{ pattern.getName() }}</a></li>
  {% endfor %}
</ul>
