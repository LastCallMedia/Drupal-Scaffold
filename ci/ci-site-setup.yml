commands:
# Install Drupal
  - command: site:install
    options:
      uri: http://default/
      force: true
    arguments:
      profile: minimal
  - command: theme:install
    options:
      set-default: true
    arguments:
      theme: [scaffold]
